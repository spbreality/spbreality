<h1>Room creation</h1>

<form action="{{ path('admin_room_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_room') }}">
            Back to the list
        </a>
    </li>
</ul>
