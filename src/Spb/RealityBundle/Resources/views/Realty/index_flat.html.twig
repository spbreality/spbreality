{% extends 'SpbRealityBundle:Realty:index.html.twig' %}

{% block rtype_ru_pl_camel %}Квартиры{% endblock %}

{% block rtypeblock %}
<table class="table table-striped table-bordered table-condensed">
    <thead>
        <tr>
            <th>№</th>
            <th colspan="2">Тип</th>
            <th>Район</th>
            <th>Адрес</th>
            <th>К</th>
            <th>Этаж</th>
            <th>Дом</th>
            <th>общ.</th>
            <th>жил.</th>
            <th>кух.</th>
            <th title="Санузел">С/У</th>
            <th>Цена</th>
            <th>Доп.сведения</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('admin_realty_show', { 'rtype': entity.realtytype, 'id': entity.id }) }}" title="Номер объекта">{{ entity.id }}</a></td>            
            <td><a href="#" title="{{ entity.operation }}">{{ entity.operation.abbr }}</a></td>
            <td><a href="#" title="{{ entity.buildingstage.name }}">{{ entity.buildingstage.abbr }}</a></td>            
            <td>{{ entity.district }}</td>
            <td>{{ entity.address }}</td>          
            <td>{{ entity.rooms }}</td>
            <td>{{ entity.floor }}/{{ entity.floors }}</td>
            <td><a href="#" title="{{ entity.buildingtype.name }}">{{ entity.buildingtype.abbr }}</a></td>           
            <td>{{ entity.s }}</td>
            <td>{{ entity.sl }}</td>
            <td>{{ entity.sk }}</td>
            <td><a href="#" title="{{ entity.bathunittype.name }}">{{ entity.bathunittype.abbr }}</a></td>
            <td>{{ entity.price }}</td>            
            <td>{{ entity.sdesc }}</td>
            <td>
                <a href="{{ path('admin_realty_show', { 'rtype': entity.realtytype, 'id': entity.id }) }}" class="icon-check" title="Показать"></a> | 
                <a href="{{ path('admin_realty_edit', { 'rtype': entity.realtytype, 'id': entity.id }) }}" class="icon-edit" title="Редактировать"></a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>


<a href="{{ path('admin_realty_new', { 'rtype': "flat" }) }}"> Добавить квартиру</a>

{% endblock %}