<a href="#">Добавить фотографию</a>

<form action="{{ path('admin_document_create') }}" method="post" {{ form_enctype(form) }} class="well form-inline">
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>